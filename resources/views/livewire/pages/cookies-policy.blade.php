<section class="container mx-auto px-4 py-12 text-gray-900">
    <h1 class="text-4xl font-extrabold mb-6 text-center">{{ __('cookies.title') }}</h1>

    <p class="mb-4">{{ __('cookies.intro') }}</p>

    <h2 class="text-2xl font-bold mt-6 mb-2">{{ __('cookies.what_title') }}</h2>
    <p class="mb-4">{{ __('cookies.what_text') }}</p>

    <h2 class="text-2xl font-bold mt-6 mb-2">{{ __('cookies.types_title') }}</h2>
    <ul class="list-disc list-inside mb-4">
        <li>{{ __('cookies.types.necessary') }}</li>
        <li>{{ __('cookies.types.functional') }}</li>
        <li>{{ __('cookies.types.analytics') }}</li>
        <li>{{ __('cookies.types.marketing') }}</li>
    </ul>

    <h2 class="text-2xl font-bold mt-6 mb-2">{{ __('cookies.control_title') }}</h2>
    <p class="mb-4">{{ __('cookies.control_text') }}</p>

    <h2 class="text-2xl font-bold mt-6 mb-2">{{ __('cookies.contacts_title') }}</h2>
    <p>
        {{ __('cookies.contacts_text') }}
        <a href="mailto:casinoritz@casino-ritz.eu" class="text-ritz-red font-bold">
            casinoritz@casino-ritz.eu
        </a>
    </p>
</section>
